const cash=c=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(c/100);fetch('/api/products').then(r=>r.json()).then(products=>{document.querySelector('#products').innerHTML=products.map((p,i)=>`<article><div class="visual tone-${i%3}"><img src="${p.image}" alt="${p.name}"><span>${p.inventory} in stock</span></div><p class="eyebrow">${p.category}</p><h3>${p.name}</h3><p class="desc">${p.description}</p><div class="buy"><b>${cash(p.priceCents)}</b><a href="/product.html?id=${p.id}">View details <span>↗</span></a></div></article>`).join('')});
