import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {addToCart,cartView,completeCheckout,createCheckout,getCart,getCheckout,getProduct,money,searchProducts} from './store.js';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../public'),port=Number(process.env.PORT||3000),base=process.env.PUBLIC_BASE_URL||`http://localhost:${port}`;
const tools=[{name:'search_products',title:'Search products',description:'Search the Sean Shop catalog for physical products.',inputSchema:{type:'object',properties:{query:{type:'string'},maxPriceDollars:{type:'number',exclusiveMinimum:0}},additionalProperties:false},annotations:{readOnlyHint:true}},{name:'get_product',title:'Get product',description:'Get current product details and inventory.',inputSchema:{type:'object',properties:{productId:{type:'string'}},required:['productId'],additionalProperties:false},annotations:{readOnlyHint:true}},{name:'add_to_cart',title:'Add product to cart',description:'Add a product to a cart, creating one when cartId is omitted.',inputSchema:{type:'object',properties:{cartId:{type:'string'},productId:{type:'string'},quantity:{type:'integer',minimum:1,maximum:10}},required:['productId'],additionalProperties:false}},{name:'get_cart',title:'Get cart',description:'Review cart items and totals.',inputSchema:{type:'object',properties:{cartId:{type:'string'}},required:['cartId'],additionalProperties:false},annotations:{readOnlyHint:true}},{name:'checkout_session',title:'Start checkout',description:'Create a hosted Sean Shop checkout. The hosted page starts ThreatMetrix profiling. It does not charge the user.',inputSchema:{type:'object',properties:{cartId:{type:'string'}},required:['cartId'],additionalProperties:false}},{name:'get_order_status',title:'Get order status',description:'Check whether a hosted checkout was completed.',inputSchema:{type:'object',properties:{checkoutId:{type:'string'}},required:['checkoutId'],additionalProperties:false},annotations:{readOnlyHint:true}}];
const ok=(text,structuredContent={})=>({content:[{type:'text',text}],structuredContent});function callTool(name,a={}){try{switch(name){case'search_products':{const products=searchProducts(a.query||'',a.maxPriceDollars?Math.round(a.maxPriceDollars*100):undefined).map(p=>({...p,price:money(p.priceCents),url:`${base}/product.html?id=${p.id}`}));return ok(products.length?`Found ${products.length} matching products.`:'No matching products found.',{products})}case'get_product':{const p=getProduct(a.productId);if(!p)throw Error('Product not found');return ok(`${p.name} costs ${money(p.priceCents)} and has ${p.inventory} units available.`,{product:{...p,price:money(p.priceCents)}})}case'add_to_cart':{const cart=cartView(addToCart(a.cartId,a.productId,a.quantity||1));return ok(`Cart updated. Total is ${money(cart.totalCents)}. Cart ID: ${cart.id}`,{cart})}case'get_cart':{const c=getCart(a.cartId);if(!c)throw Error('Cart not found');const cart=cartView(c);return ok(`Cart total: ${money(cart.totalCents)}.`,{cart})}case'checkout_session':{const x=createCheckout(a.cartId),cart=cartView(getCart(a.cartId)),url=`${base}/checkout.html?checkoutId=${x.id}`;return ok(`Checkout is ready: ${url}. Opening it starts ThreatMetrix profiling. The user must explicitly place the demo order there.`,{checkout:{id:x.id,status:x.status,url,cart}})}case'get_order_status':{const x=getCheckout(a.checkoutId);if(!x)throw Error('Checkout not found');return ok(`Checkout status: ${x.status}.`,{checkout:x})}default:throw Error('Unknown tool')}}catch(e){return{isError:true,content:[{type:'text',text:e.message}]}}}
function json(res,status,obj,headers={}){res.writeHead(status,{'content-type':'application/json',...headers});res.end(JSON.stringify(obj))}async function body(req){let text='';for await(const c of req)text+=c;return text?JSON.parse(text):{}}const mime={'.html':'text/html; charset=utf-8','.js':'text/javascript; charset=utf-8','.css':'text/css; charset=utf-8','.svg':'image/svg+xml'};
http.createServer(async(req,res)=>{try{const u=new URL(req.url,base);if(u.pathname==='/mcp'){if(req.method!=='POST')return json(res,405,{error:'MCP endpoint accepts POST JSON-RPC requests.'},{allow:'POST'});const r=await body(req),id=r.id??null,headers={'mcp-protocol-version':'2025-06-18'};let result;if(r.method==='initialize')result={protocolVersion:'2025-06-18',capabilities:{tools:{}},serverInfo:{name:'sean-shop',version:'1.0.0'},instructions:'Help users shop Sean Shop, maintain a cart, and start hosted checkout. Never claim an order is paid until checkout completes.'};else if(r.method==='tools/list')result={tools};else if(r.method==='tools/call')result=callTool(r.params?.name,r.params?.arguments);else if(r.method==='notifications/initialized'){res.writeHead(202,headers);return res.end()}else return json(res,200,{jsonrpc:'2.0',id,error:{code:-32601,message:'Method not found'}},headers);return json(res,200,{jsonrpc:'2.0',id,result},headers)}if(u.pathname==='/health')return json(res,200,{ok:true});if(u.pathname==='/api/products')return json(res,200,searchProducts(u.searchParams.get('q')||''));if(u.pathname==='/api/config'){const orgId=process.env.TMX_ORG_ID||'';return json(res,200,{tmxDomain:process.env.TMX_PROFILING_DOMAIN||'h.online-metrix.net',tmxOrgId:orgId,tmxConfigured:/^[A-Za-z0-9]{8}$/.test(orgId)})}let m=u.pathname.match(/^\/api\/checkout\/([^/]+)$/);if(req.method==='GET'&&m){const x=getCheckout(m[1]);return x?json(res,200,{checkout:x,cart:cartView(getCart(x.cartId))}):json(res,404,{error:'Not found'})}m=u.pathname.match(/^\/api\/checkout\/([^/]+)\/complete$/);if(req.method==='POST'&&m){const x=getCheckout(m[1]);if(!x)return json(res,404,{error:'Not found'});return json(res,200,{checkout:completeCheckout(m[1]),orderNumber:`DEMO-${Date.now()}`})}const file=u.pathname==='/'?'/index.html':u.pathname,full=path.resolve(root,'.'+file);if(!full.startsWith(root)||!fs.existsSync(full)||fs.statSync(full).isDirectory()){res.writeHead(404);return res.end('Not found')}res.writeHead(200,{'content-type':mime[path.extname(full)]||'application/octet-stream'});fs.createReadStream(full).pipe(res)}catch(e){json(res,500,{error:e.message})}}).listen(port,()=>console.log(`Sean Shop: ${base}\nMCP: ${base}/mcp`));
